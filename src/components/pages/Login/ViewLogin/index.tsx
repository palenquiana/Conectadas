import { Layout } from "../../../common";
import { FormLogin } from "../FormLogin";

const ViewLogin = () => {
  return (
    <Layout hideNav page="login">
      <FormLogin />
    </Layout>
  );
};
export { ViewLogin };
